# Summary: Show Resource Name at Top of Overview Tab in All Detail Panels

**ID:** 260413-1z2
**Completed:** 2026-04-13
**Commit:** a5a1c0d

## What Changed

Added a prominent resource name heading at the top of the Overview tab content area in all three detail panels, before the stat cards grid. Each heading includes:

- Resource name as `text-base font-semibold` (full display, wraps if needed)
- Resource type label in `text-[11px]` muted text

### VMDetailPanel.tsx

- Heading shows `resourceName` with "Virtual Machine" type label
- Added `vm_type` badge (Azure VM / Arc VM) next to type label — blue for Azure VM, purple for Arc VM

### VMSSDetailPanel.tsx

- Heading shows `resourceName` with "Virtual Machine Scale Set" type label

### AKSDetailPanel.tsx

- Heading shows `resourceName` with "AKS Cluster" type label

### PatchDetailPanel — No change needed

Already shows `machine.machineName` prominently as `<h2>` in its header, and has no overview tab.

## Verification

- `npx tsc --noEmit` — passes (only pre-existing `.next/types` cache warnings)
- `npm run build` — passes cleanly, all routes compiled

## Acceptance Criteria

- [x] All 3 panels show resource name at top of Overview tab content
- [x] Resource type label shown below the name
- [x] Name is not truncated in the content area (full display, wrapping if needed)
- [x] `npx tsc --noEmit` passes
- [x] `npm run build` passes
