# Summary: 260409-00u — Show Connection Status Instead of Power State for Arc VMs

**Status:** COMPLETE  
**Commit:** `83f5c4a`

## Changes Made

### Task 1 — Backend (`services/api-gateway/vm_inventory.py`)
Replaced the 2-line power state extraction block with vm_type-aware logic in the `list_vms()` response loop (lines 427–438).

- `vm_type` is now extracted from the row before being used in both the branch logic and the response dict (no duplication — the previous `row.get("vmType", "Azure VM")` at the dict level is replaced by the already-bound `vm_type` variable).
- Arc VMs: `"Connected"` → `"connected"`, `"Disconnected"` → `"disconnected"`, anything else → `"unknown"`. Uses substring matching per constraints to handle any casing variants (e.g. `"Connected (Agent)"` still resolves to `"connected"`).
- Azure VMs: unchanged — still routed through `_normalize_power_state()`.

### Task 2 — Frontend (`services/web-ui/components/VMTab.tsx`)
Added `connected` and `disconnected` entries to the `PowerStateBadge` config map (lines 35–36).

- `connected` → green badge labelled "Connected" (same colour as "Running")
- `disconnected` → red badge labelled "Disconnected"
- All existing entries and the fallback `??` path are unchanged.

## Verification

- Azure VMs: `_normalize_power_state()` path untouched — running/stopped/deallocated unaffected.
- Arc VMs: badge now shows green "Connected" or red "Disconnected" instead of grey "Unknown".
- No regressions introduced — only the two targeted lines changed in each file.
