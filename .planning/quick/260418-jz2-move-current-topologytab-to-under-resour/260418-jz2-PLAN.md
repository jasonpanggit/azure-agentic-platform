# Plan: Move TopologyTab to Resources Hub as Resource Hierarchy

**ID:** 260418-jz2  
**Mode:** quick  
**Status:** ready

## Objective

Move the existing `TopologyTab` (subscription → resource group → resource tree) out of `NetworkHubTab` and into `ResourcesHubTab` as a new "Resource Hierarchy" sub-tab.

## Tasks

### Task 1 — Rename TopologyTab component file and export

- Copy `services/web-ui/components/TopologyTab.tsx` → `services/web-ui/components/ResourceHierarchyTab.tsx`
- Rename exported function from `TopologyTab` to `ResourceHierarchyTab`
- Delete `services/web-ui/components/TopologyTab.tsx`

### Task 2 — Add Resource Hierarchy sub-tab to ResourcesHubTab

File: `services/web-ui/components/ResourcesHubTab.tsx`

- Add import: `import { ResourceHierarchyTab } from './ResourceHierarchyTab'`
- Add icon import: `GitBranch` from `lucide-react`
- Append to `subTabs` array: `{ id: 'resource-hierarchy', label: 'Resource Hierarchy', icon: GitBranch }`
- Add render block: `{activeSubTab === 'resource-hierarchy' && <ResourceHierarchyTab subscriptions={subscriptions} />}`

### Task 3 — Remove Topology sub-tab from NetworkHubTab

File: `services/web-ui/components/NetworkHubTab.tsx`

- Remove `import { TopologyTab } from './TopologyTab'`
- Remove `{ id: 'topology', label: 'Topology', icon: Network }` from `subTabs` array
- Change `initialSubTab` default from `'topology'` to `'vnet-peerings'`
- Remove render block: `{activeSubTab === 'topology' && <TopologyTab ... />}`
- Remove unused `Network` icon import if no longer used elsewhere in file

## Acceptance Criteria

- [ ] `TopologyTab.tsx` no longer exists; `ResourceHierarchyTab.tsx` exists with renamed export
- [ ] ResourcesHubTab shows "Resource Hierarchy" as last sub-tab, renders the tree
- [ ] NetworkHubTab has no Topology sub-tab; defaults to `vnet-peerings`
- [ ] No broken imports anywhere
