# Quick Plan: Move Chat Button and Panel from Right to Left

**ID:** 260411-wm8
**Date:** 2026-04-11
**Status:** planned

## Objective

Move the chat toggle FAB button and chat drawer panel from the right side of the screen to the left side.

## Files to Change

| File | Change |
|------|--------|
| `services/web-ui/components/ChatFAB.tsx` | Change `right-6` → `left-6` in fixed position classes |
| `services/web-ui/components/ChatDrawer.tsx` | Change `right-0` → `left-0`, flip shadow direction, flip resize handle position and slide direction, flip `borderLeft` → `borderRight` |
| `services/web-ui/lib/use-resizable.ts` | Flip resize delta direction: dragging right increases width (currently `startX - e.clientX`, change to `e.clientX - startX`) |

## Tasks

- [ ] **Task 1 — Move ChatFAB to bottom-left**
  - In `ChatFAB.tsx` line 16: change `right-6` to `left-6` in the `className` string
  - No other changes needed in this file

- [ ] **Task 2 — Move ChatDrawer panel to left side**
  - In `ChatDrawer.tsx`:
    - Line 257 drawer div: change `right-0` → `left-0`
    - Line 265: change `borderLeft` → `borderRight` (border is now on the right edge)
    - Line 266: change box-shadow `-4px 0 24px` → `4px 0 24px` (shadow now casts rightward)
    - Line 267 transform: `translateX(100%)` is incorrect for a left panel — change to `translateX(-100%)`
    - Line 270 resize handle div: change `left-0` → `right-0` (handle moves to the right edge of the panel)

- [ ] **Task 3 — Fix resize direction in `use-resizable.ts`**
  - Line 63 comment: update to "Drawer is on the left side — dragging right increases width"
  - Line 64: flip delta direction from `startX.current - e.clientX` → `e.clientX - startX.current`

## Acceptance Criteria

- FAB appears at bottom-left when drawer is closed
- Chat drawer slides in from the left
- Backdrop still covers the remaining viewport
- Resize handle is on the right edge of the drawer; dragging right expands, dragging left shrinks
- `npm run build` exits 0 with no TypeScript errors
