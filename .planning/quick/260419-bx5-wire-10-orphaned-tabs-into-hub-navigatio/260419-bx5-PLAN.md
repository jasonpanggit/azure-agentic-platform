# Quick Task 260419-bx5: Wire Orphaned Tabs Into Hub Navigation + Fix DeploymentTab Bug

**Goal:** Make 9 fully-built but unreachable tabs accessible from their logical hub tabs, and fix the `resourceGroup={undefined}` bug in ChangeHubTab.

## Task 1: Add NsgAuditTab to NetworkHubTab

**File:** `services/web-ui/components/NetworkHubTab.tsx`

Add `NsgAuditTab` as a 5th sub-tab ("NSG Audit").

- Import `NsgAuditTab`
- Add `{ id: 'nsg-audit', label: 'NSG Audit', icon: ShieldAlert }` to subTabs
- Add render: `{activeSubTab === 'nsg-audit' && <NsgAuditTab subscriptions={subscriptions} />}`

## Task 2: Add PolicyComplianceTab + AlertCoverageTab + VMExtensionAuditTab to SecurityHubTab

**File:** `services/web-ui/components/SecurityHubTab.tsx`

Add 3 new sub-tabs:
- `PolicyComplianceTab` → `{ id: 'policy', label: 'Policy', icon: FileText }`
- `AlertCoverageTab` → `{ id: 'alert-coverage', label: 'Alert Coverage', icon: BellRing }`
- `VMExtensionAuditTab` → `{ id: 'vm-extensions', label: 'VM Extensions', icon: Puzzle }`

## Task 3: Add CostAnomalyTab to CostHubTab

**File:** `services/web-ui/components/CostHubTab.tsx`

Add 1 new sub-tab:
- `CostAnomalyTab` → `{ id: 'anomalies', label: 'Cost Anomalies', icon: TrendingUp }`
- Render: `{activeSubTab === 'anomalies' && <CostAnomalyTab subscriptions={subscriptions} />}`

## Task 4: Add LockAuditTab + TaggingComplianceTab to AuditHubTab

**File:** `services/web-ui/components/AuditHubTab.tsx`

Add 2 new sub-tabs:
- `LockAuditTab` → `{ id: 'locks', label: 'Lock Audit', icon: Lock }`
- `TaggingComplianceTab` → `{ id: 'tagging', label: 'Tagging', icon: Tag }`

Note: `LockAuditTab` needs `subscriptions` prop. `TaggingComplianceTab` takes `subscriptionId`.

## Task 5: Add AppServiceHealthTab + QueueDepthTab to ResourcesHubTab

**File:** `services/web-ui/components/ResourcesHubTab.tsx`

Add 2 new sub-tabs:
- `AppServiceHealthTab` → `{ id: 'app-services', label: 'App Services', icon: Globe2 }`
- `QueueDepthTab` → `{ id: 'messaging', label: 'Messaging', icon: MessageSquare }`

## Task 6: Fix DeploymentTab resourceGroup bug in ChangeHubTab

**File:** `services/web-ui/components/ChangeHubTab.tsx`

Change:
```tsx
{activeSubTab === 'deployments' && <DeploymentTab resourceGroup={undefined} />}
```
To:
```tsx
{activeSubTab === 'deployments' && <DeploymentTab subscriptions={subscriptions} />}
```

Check `DeploymentTab` props interface first — if it accepts `subscriptions`, use that. If it only accepts `resourceGroup`, pass `subscriptions[0]` or remove the prop entirely and let the component manage its own subscriptions via context.
