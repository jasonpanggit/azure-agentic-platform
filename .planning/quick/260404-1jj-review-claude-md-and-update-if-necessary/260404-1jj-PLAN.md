# Quick Task: Review CLAUDE.md and Update If Necessary

**Task ID:** 260404-1jj
**Created:** 2026-04-04
**Status:** PLANNED

---

## Analysis — Gaps Found in CLAUDE.md

After comparing CLAUDE.md against current project state (STATE.md, memory files, ROADMAP.md), the following gaps are significant:

### 1. STALE: Frontend Section Still Says "Fluent UI 2"

**Current CLAUDE.md (line 160):** `## Frontend (Next.js + Fluent UI 2)` and the entire Fluent UI 2 subsection (lines 176-188) describes `@fluentui/react-components v9.73.4` with Griffel styling.

**Reality:** Fluent UI was **completely removed** in Phase 9 (2026-03-31). The frontend now uses:
- **Tailwind CSS v3.4.19** (utility-first, not CSS-in-JS)
- **shadcn/ui** (New York preset, 18 components in `components/ui/`)
- **lucide-react** for icons
- No Griffel, no FluentProvider, no `@fluentui/react-components`

This is the **most critical** staleness issue — CLAUDE.md actively recommends a UI library that was removed.

### 2. STALE: "What NOT to Use" Section Needs Update

The "What NOT to use" table entry for `@fluentui/react v8` is fine, but there's no entry warning against `@fluentui/react-components v9` now that it's been removed. The Fluent UI v9 still appears in the "Summary: Versions At a Glance" table (line 305) as if it's in use.

### 3. EMPTY: Conventions Section

**Current:** `"Conventions not yet established. Will populate as patterns emerge during development."`

**Reality:** 21 phases of development have established **many** conventions. Key patterns from memory files:
- `Optional[X]` over `X | None` in FastAPI signatures (Python 3.9 compat)
- `pythonpath=["."]` in pyproject.toml for pytest imports
- Conftest.py hyphenated package shim pattern
- Module-level SDK scaffold pattern (try/except ImportError, `_log_sdk_availability`, `_extract_subscription_id`)
- Tool function pattern (start_time = time.monotonic(), duration_ms in both try/except, never raises)
- Proxy route pattern (getApiGatewayUrl + buildUpstreamHeaders + AbortSignal.timeout(15000))
- CSS semantic token system (var(--accent-*), color-mix pattern, no hardcoded Tailwind colors)
- `det-` prefix on detection-plane incident IDs
- Internal-only Container Apps for MCP servers
- MCP connection `category = "CustomKeys"` (NOT `"MCP"`)
- Gateway as thin router only (no business logic)
- Fire-and-forget for non-critical async operations
- ETag optimistic concurrency in Cosmos DB

### 4. EMPTY: Architecture Section

**Current:** `"Architecture not yet mapped. Follow existing patterns found in the codebase."`

**Reality:** The architecture is well-documented across memory files. Key elements:
- 9 domain agents (Orchestrator + 8 specialists) on Container Apps
- API Gateway (FastAPI) as thin router
- Azure MCP Server + Custom Arc MCP Server (both internal-only)
- Foundry Agent Service for conversation threading
- Cosmos DB (incidents, sessions, approvals) + PostgreSQL (runbooks, pgvector RAG)
- Fabric Eventhouse + Activator detection plane
- Next.js web UI + Teams bot as dual interaction surfaces
- SSE streaming for real-time updates

### 5. STALE: Project Description

Line 6 still says "Fluent UI 2 + Next.js" — should be "Tailwind CSS + shadcn/ui + Next.js"

### 6. STALE: Handoff Pattern Reference

Line 49 mentions only 6 agents for Handoff: `Compute/Network/Storage/Security/Arc/SRE`. Reality is 8 domain agents (adds Patch + EOL).

### 7. NOT STALE (acceptable)

- Technology stack version numbers — still accurate (these are the versions researched at project start)
- Terraform provider versions — unchanged
- "What NOT to Use" table — all entries still valid
- GSD Workflow section — still accurate
- Teams SDK section — unchanged

---

## Tasks

### Task 1: Update Frontend Section + Project Description

- [ ] Replace "Fluent UI 2 + Next.js" with "Tailwind CSS + shadcn/ui + Next.js" in project description (line 6)
- [ ] Replace the Fluent UI 2 subsection (lines 176-188) with a Tailwind CSS + shadcn/ui section documenting the actual current stack
- [ ] Update the section header from `Frontend (Next.js + Fluent UI 2)` to `Frontend (Next.js + Tailwind CSS + shadcn/ui)`
- [ ] Add `@fluentui/react-components v9` to the "What NOT to use" table (it was removed in Phase 9)
- [ ] Update "Summary: Versions At a Glance" table: replace Fluent UI row with Tailwind + shadcn/ui row
- [ ] Update Handoff pattern to mention all 8 domain agents

### Task 2: Populate Conventions Section

- [ ] Replace placeholder text with documented conventions from memory files
- [ ] Group by category: Python, Frontend, Terraform, API patterns

### Task 3: Populate Architecture Section

- [ ] Replace placeholder text with a concise architecture summary
- [ ] Include: agent topology, data stores, detection plane, interaction surfaces
- [ ] Keep it brief — reference existing docs for deep dives

---

## Acceptance Criteria

- [ ] No references to Fluent UI as the active UI framework
- [ ] Conventions section has real, actionable content
- [ ] Architecture section has a useful overview
- [ ] All GSD markers (`<!-- GSD:xxx-start -->`) preserved intact
- [ ] Project description matches reality
