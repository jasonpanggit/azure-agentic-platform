# Quick Task 260414-o65 — Fix 2 Pre-existing CI Failures

**Branch:** `gsd/phase-50-cross-subscription-federated-view`
**Completed:** 2026-04-14
**Commits:** 2

---

## Summary

Fixed two CI checks that have been failing on every PR since Phase 49.

---

## Task 1: Terraform Plan (prod) — 3 variables with no default

**Commit:** `6a1054f` — `fix(terraform): add default empty string to client_id, client_secret, fabric_admin_email`

**File changed:** `terraform/envs/prod/variables.tf`

Added `default = ""` to three variables that had no default value:
- `client_id` (kept `sensitive = true`) — used by azurerm/azapi/azuread providers; empty is valid with OIDC (`ARM_USE_OIDC=true`)
- `client_secret` (kept `sensitive = true`) — same rationale
- `fabric_admin_email` — used in fabric module; empty string is safe for plan-time validation

Production behaviour is unchanged: `credentials.tfvars` and `TF_VAR_*` env vars still override at apply time.

---

## Task 2: Verify Agent Spec Files — 4 missing spec files

**Commit:** `8e782d5` — `docs(agents): add missing spec files for appservice, containerapps, database, messaging agents`

**Files created:**
- `docs/agents/appservice-agent.spec.md`
- `docs/agents/containerapps-agent.spec.md`
- `docs/agents/database-agent.spec.md`
- `docs/agents/messaging-agent.spec.md`

Each file follows the canonical format from `docs/agents/compute-agent.spec.md`:
- YAML frontmatter (`agent`, `requirements`, `phase`)
- All 6 required sections: `## Persona`, `## Goals`, `## Workflow`, `## Tool Permissions`, `## Safety Constraints`, `## Example Flows`
- 3 example flows per agent with realistic diagnostic scenarios
- Domain-accurate tool allowlists and @ai_function tool listings derived from each agent's `tools.py`
- HITL safety constraints consistent with REMEDI-001

---

## CI Checks Fixed

| Check | Root Cause | Fix |
|---|---|---|
| **Plan (prod) — Terraform** | 3 variables had no default; plan failed without tfvars | Added `default = ""` to `client_id`, `client_secret`, `fabric_admin_email` |
| **Verify Agent Spec Files** | 4 spec files missing from `docs/agents/` | Created all 4 with correct format and all 6 required sections |
