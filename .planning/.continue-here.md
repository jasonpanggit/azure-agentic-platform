---
context: operator-actions
phase: backlog-remediation
status: in_progress
last_updated: 2026-04-10T16:22:19.557Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: ACR build context — Dockerfile uses `COPY services/api-gateway/requirements.txt` so build context MUST be repo root (`.`), NOT `services/api-gateway/`. Run ch6a failed because of this.
- [ ] CONSTRAINT: terraform apply requires enabling storage + KV public access first — script handles this but KV must be restored to Disabled after.
- [ ] CONSTRAINT: PostgreSQL has no public access — `az vm create` fails with JSON parse error in rg-aap-prod subnets. Use GitHub Actions runner (already in VNet) for seeding instead.

**Do not proceed until all boxes are checked.**

<current_state>
Audited all Phase 8 BACKLOG.md items. All 11 code-level findings are resolved in Phases 9-28.
Running 3 operator actions:
1. ✅ terraform apply — COMPLETE (2 added, 7 changed, 2 destroyed; rbac_assignment_count=30)
2. ⏳ Seed runbooks — BLOCKED (VM creation fails; need GitHub Actions runner approach)
3. ⏳ Rebuild ca-api-gateway-prod — IN PROGRESS (ACR build ch6b uploading at pause time)
</current_state>

<completed_work>
- Audited all 12 BACKLOG.md items (F-01 through F-11 + OTel) against live codebase
- Rewrote BACKLOG.md: all resolved items moved to RESOLVED section, 5 operator actions remain
- Ran `bash scripts/ops/terraform-local-apply.sh prod -auto-approve` — succeeded
  - Activated: Azure AI Developer RBAC on Foundry (F-01), MCP connections for Azure+Arc (F-09/F-10/F-11), TEAMS_CHANNEL_ID wiring (F-04), CORS (F-03)
  - Key Vault public access restored to Disabled after apply
</completed_work>

<remaining_work>
**Task A: Seed runbooks (pgvector)**
- BLOCKED: `az vm create` fails with `ERROR: Extra data: line 1 column 4 (char 3)` on both subnets
- FIX: Trigger seeding via GitHub Actions workflow (runner is inside VNet)
  - Option 1: Manually dispatch `seed-runbooks` workflow if it exists in .github/workflows/
  - Option 2: Create a one-off GH Actions job that runs `bash scripts/ops/19-4-seed-runbooks.sh`
  - Option 3: Check if jumphost (aml-rg, southeastasia) has VNet peering to prod VNet and can reach `aap-postgres-prod.postgres.database.azure.com`
- Postgres creds: `postgresql://aap_admin:Jas190277on!@aap-postgres-prod.postgres.database.azure.com:5432/aap?sslmode=require`

**Task B: Rebuild ca-api-gateway-prod Container App (OTel)**
- ACR build triggered: `az acr build --registry aapcrprodjgmjti --image services/api-gateway:dbbdd068a67e35a63862a1045d4ecf02afbd8b6f --agent-pool aap-builder-prod --file services/api-gateway/Dockerfile .`
- Check status: `az acr task logs --registry aapcrprodjgmjti` (shows latest run)
- If build succeeded: Container App may auto-update (check revision mode). If not:
  `az containerapp update --name ca-api-gateway-prod --resource-group rg-aap-prod --image aapcrprodjgmjti.azurecr.io/services/api-gateway:dbbdd068a67e35a63862a1045d4ecf02afbd8b6f`
- Verify: Check App Insights for `foundry.*`, `mcp.*`, `agent.orchestrator` span types

**Task C: Human-only (no automation possible)**
- Add GitHub Actions staging secrets: E2E_CLIENT_ID, E2E_CLIENT_SECRET, E2E_API_AUDIENCE → see docs/ops/e2e-service-principal.md
- Teams Bot channel install → see scripts/ops/19-5-package-manifest.sh
</remaining_work>

<decisions_made>
- terraform-local-apply.sh used (handles ARM_* env vars + KV firewall toggle)
- ACR build context = repo root (.) — Dockerfile has COPY paths relative to repo root
- Jumphost is in aml-rg/southeastasia — different region, may not reach prod VNet
</decisions_made>

<blockers>
- az vm create JSON parse error on both subnets in rg-aap-prod — root cause unknown; use GH Actions runner instead
- ACR build status unknown at pause time (was still uploading)
</blockers>

## Infrastructure State
- **Key Vault kv-aap-prod**: public access = Disabled (restored after terraform apply)
- **tfstate storage staaptfstateprod**: public access = Enabled (not restored — check if needed)
- **terraform apply**: completed successfully, state locked/released
- **ACR build**: ch6b queued/running at pause time (check portal or `az acr task logs --registry aapcrprodjgmjti`)
- **Background process bf7hk20qp**: seed-via-vm.sh — FAILED at VM creation step, process terminated by cleanup handler

## Required Reading
1. `.planning/BACKLOG.md` — updated with current state of all items
2. `.planning/HANDOFF.json` — machine-readable full state

<context>
This session was a backlog cleanup: audit + fix. All code is already correct (Phases 9-28 fixed everything). 
The only real work left is operator plumbing: seed DB, rebuild container, add GH secrets, install Teams bot.
terraform apply got the heavy lifting done (RBAC, MCP connections all live now).
The two blocking items (seed + OTel rebuild) are just execution tasks, not design decisions.
</context>

<next_action>
1. Check ACR build: `az acr task logs --registry aapcrprodjgmjti` — if succeeded, update Container App
2. Fix seed: check .github/workflows/ for a runbook-seeding workflow; if none, create one-off job using self-hosted runner
3. Check staaptfstateprod public access (may need restoring to Disabled)
</next_action>
