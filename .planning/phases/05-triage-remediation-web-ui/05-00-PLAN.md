# 05-00: Wave 0 — Test Infrastructure & Stubs

```yaml
wave: 0
depends_on: []
files_modified:
  - services/web-ui/package.json                              # NEW
  - services/web-ui/tsconfig.json                             # NEW
  - services/web-ui/next.config.ts                            # NEW
  - services/web-ui/playwright.config.ts                      # NEW
  - services/web-ui/.env.example                              # NEW
  - services/api-gateway/tests/conftest.py                    # NEW
  - services/api-gateway/tests/test_chat_endpoint.py          # NEW (stub)
  - services/api-gateway/tests/test_approval_lifecycle.py     # NEW (stub)
  - services/api-gateway/tests/test_resource_identity.py      # NEW (stub)
  - services/api-gateway/tests/test_rate_limiting.py          # NEW (stub)
  - services/api-gateway/tests/test_gitops_path.py            # NEW (stub)
  - services/api-gateway/tests/test_runbook_rag.py            # NEW (stub)
  - services/api-gateway/tests/test_sse_stream.py             # NEW (stub)
  - services/api-gateway/tests/test_sse_heartbeat.py          # NEW (stub)
  - services/api-gateway/tests/test_audit_trail.py            # NEW (stub)
  - services/web-ui/__tests__/layout.test.tsx                 # NEW (stub)
  - services/web-ui/__tests__/auth.test.tsx                   # NEW (stub)
  - e2e/sc1.spec.ts                                           # NEW (stub)
  - e2e/sc2.spec.ts                                           # NEW (stub)
  - e2e/sc5.spec.ts                                           # NEW (stub)
  - e2e/sc6.spec.ts                                           # NEW (stub)
  - pyproject.toml                                            # MODIFY — add markers
autonomous: true
requirements:
  - UI-001
  - UI-002
  - TRIAGE-005
  - TRIAGE-007
  - UI-008
  - REMEDI-002
  - REMEDI-003
  - REMEDI-004
  - REMEDI-005
  - REMEDI-006
  - REMEDI-008
  - AUDIT-002
  - AUDIT-004
```

---

## Goal

Scaffold all test infrastructure for Phase 5: Next.js project skeleton with `package.json` (Fluent UI v9, MSAL, Playwright), Playwright config, shared pytest conftest with mock fixtures (Foundry client, Cosmos DB, ARM client, pgvector embeddings, Teams notifier), and all Wave 0 test stub files referenced in VALIDATION.md. Every subsequent plan in Phase 5 starts with passing stub tests and implements until they go green.

---

## Tasks

### Task 5-00-01: Create Web UI package.json and TypeScript Config

<read_first>
- CLAUDE.md §"Frontend (Next.js + Fluent UI 2)" — version pinning and SSR notes
- .planning/phases/05-triage-remediation-web-ui/05-RESEARCH.md §12 (package matrix)
- .planning/phases/05-triage-remediation-web-ui/05-UI-SPEC.md §"Design System" (packages)
- services/api-gateway/Dockerfile (existing service Dockerfile pattern)
</read_first>

<action>
Create `services/web-ui/package.json` with these exact dependencies:

```json
{
  "name": "aap-web-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@fluentui/react-components": "^9.73.4",
    "@fluentui/react-icons": "^2.0.0",
    "@azure/msal-browser": "^3.0.0",
    "@azure/msal-react": "^2.0.0",
    "react-resizable-panels": "^2.0.0",
    "react-markdown": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/node": "^20.0.0",
    "@playwright/test": "^1.58.2",
    "@testing-library/react": "^16.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
```

Create `services/web-ui/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
```

Create `services/web-ui/next.config.ts`:
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'standalone',
  reactStrictMode: true,
  transpilePackages: [
    '@fluentui/react-components',
    '@fluentui/react-icons',
  ],
};

export default nextConfig;
```

Create `services/web-ui/.env.example`:
```
NEXT_PUBLIC_AZURE_CLIENT_ID=
NEXT_PUBLIC_TENANT_ID=
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000/auth/callback
API_GATEWAY_URL=http://localhost:8000
```
</action>

<acceptance_criteria>
- `services/web-ui/package.json` contains `"next": "^15.0.0"`
- `services/web-ui/package.json` contains `"@fluentui/react-components": "^9.73.4"`
- `services/web-ui/package.json` contains `"@azure/msal-browser": "^3.0.0"`
- `services/web-ui/package.json` contains `"@playwright/test": "^1.58.2"`
- `services/web-ui/package.json` contains `"react-resizable-panels": "^2.0.0"`
- `services/web-ui/tsconfig.json` contains `"jsx": "preserve"`
- `services/web-ui/next.config.ts` contains `output: 'standalone'`
- `services/web-ui/next.config.ts` contains `transpilePackages` with `@fluentui/react-components`
- `services/web-ui/.env.example` contains `API_GATEWAY_URL=`
</acceptance_criteria>

---

### Task 5-00-02: Create Playwright Config

<read_first>
- .planning/phases/05-triage-remediation-web-ui/05-VALIDATION.md §"Wave 0 Requirements" (playwright.config.ts spec)
- .planning/phases/05-triage-remediation-web-ui/05-RESEARCH.md §"Validation Test File Map" (tag names)
- CLAUDE.md §"E2E Testing — Playwright" (version 1.58.2)
</read_first>

<action>
Create `services/web-ui/playwright.config.ts`:

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: '../../e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? 'github' : 'html',
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: process.env.CI ? undefined : {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: true,
    timeout: 30000,
  },
});
```

Create stub E2E test files:

`e2e/sc1.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('@sc1 Web UI Load + First Token', () => {
  test.skip('FMP under 2 seconds on cold load', async ({ page }) => {
    // TODO: Implement in Plan 05-02
  });

  test.skip('First event:token arrives within 1s of agent response start', async ({ page }) => {
    // TODO: Implement in Plan 05-02
  });
});
```

`e2e/sc2.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('@sc2 Dual SSE Reconnect', () => {
  test.skip('Monotonic sequence numbers on event:token stream', async ({ page }) => {
    // TODO: Implement in Plan 05-02
  });

  test.skip('Last-Event-ID sent on reconnect after 10s drop', async ({ page }) => {
    // TODO: Implement in Plan 05-02
  });

  test.skip('Zero duplicate sequence numbers after reconnect', async ({ page }) => {
    // TODO: Implement in Plan 05-02
  });
});
```

`e2e/sc5.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('@sc5 Resource Identity Certainty — stale_approval', () => {
  test.skip('Resource change after approval causes stale_approval abort in SSE trace', async ({ page }) => {
    // TODO: Implement in Plan 05-04
  });
});
```

`e2e/sc6.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('@sc6 GitOps vs Direct-Apply Path', () => {
  test.skip('@sc6-gitops Flux-managed cluster triggers PR creation', async ({ page }) => {
    // TODO: Implement in Plan 05-04
  });

  test.skip('@sc6-direct Non-GitOps cluster triggers direct-apply', async ({ page }) => {
    // TODO: Implement in Plan 05-04
  });
});
```
</action>

<acceptance_criteria>
- `services/web-ui/playwright.config.ts` contains `testDir: '../../e2e'`
- `services/web-ui/playwright.config.ts` contains `baseURL: process.env.BASE_URL || 'http://localhost:3000'`
- `e2e/sc1.spec.ts` contains `@sc1`
- `e2e/sc2.spec.ts` contains `@sc2`
- `e2e/sc5.spec.ts` contains `@sc5`
- `e2e/sc6.spec.ts` contains `@sc6`
- All E2E stub files contain `test.skip` (stubs, not runnable implementations)
</acceptance_criteria>

---

### Task 5-00-03: Create Shared pytest Conftest for Phase 5

<read_first>
- conftest.py (root conftest — hyphenated package shim pattern)
- services/api-gateway/tests/test_incidents.py (existing test pattern, TestClient usage)
- agents/shared/budget.py (ETag Cosmos DB mock pattern)
- services/api-gateway/foundry.py (_get_foundry_client, create_foundry_thread)
- .planning/phases/05-triage-remediation-web-ui/05-VALIDATION.md §"Wave 0 Requirements" (conftest fixture list)
</read_first>

<action>
Create `services/api-gateway/tests/conftest.py` with the following shared fixtures:

```python
"""Shared pytest fixtures for Phase 5 api-gateway tests.

Fixtures:
- client: FastAPI TestClient for the api-gateway
- mock_foundry_client: Mocked AIProjectClient (agents.create_thread, create_message, create_run)
- mock_cosmos_approvals: Mocked Cosmos DB approvals container (read_item, replace_item, create_item, query_items)
- mock_cosmos_incidents: Mocked Cosmos DB incidents container
- mock_teams_notifier: Mocked Teams card poster (post_card, update_card)
- mock_arm_client: Mocked ARM resource client for Resource Identity Certainty
- sample_approval_record: Pre-built approval record dict matching the D-12 schema
- sample_remediation_proposal: Pre-built RemediationProposal for testing
- pre_seeded_embeddings: 3 pre-computed 1536-dim float vectors for runbook RAG testing (no Azure OpenAI call)
"""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from fastapi.testclient import TestClient

from services.api_gateway.main import app


@pytest.fixture()
def client():
    """FastAPI TestClient for the api-gateway."""
    return TestClient(app)


@pytest.fixture()
def mock_foundry_client():
    """Mocked AIProjectClient with agents sub-client."""
    client = MagicMock()
    client.agents.create_thread.return_value = MagicMock(id="thread-test-001")
    client.agents.create_message.return_value = MagicMock(id="msg-test-001")
    client.agents.create_run.return_value = MagicMock(id="run-test-001")
    return client


@pytest.fixture()
def mock_cosmos_approvals():
    """Mocked Cosmos DB approvals container."""
    container = MagicMock()
    container.read_item.return_value = {
        "id": "appr_test-001",
        "action_id": "act_test-001",
        "thread_id": "thread-test-001",
        "incident_id": "inc-test-001",
        "agent_name": "compute",
        "status": "pending",
        "risk_level": "high",
        "proposed_at": "2026-03-27T14:30:00Z",
        "expires_at": "2026-03-27T15:00:00Z",
        "decided_at": None,
        "decided_by": None,
        "executed_at": None,
        "abort_reason": None,
        "resource_snapshot": {
            "resource_id": "/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01",
            "provisioning_state": "Succeeded",
            "tags": {"environment": "production"},
            "resource_health": "Available",
            "snapshot_hash": "a" * 64,
        },
        "proposal": {
            "description": "Restart VM vm-prod-01",
            "target_resources": ["/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01"],
            "estimated_impact": "~2 min downtime",
            "risk_level": "high",
            "reversibility": "reversible",
            "action_type": "restart",
        },
        "_etag": '"etag-test-001"',
    }
    container.replace_item.return_value = container.read_item.return_value
    container.create_item.return_value = container.read_item.return_value
    container.query_items.return_value = iter([])
    return container


@pytest.fixture()
def mock_cosmos_incidents():
    """Mocked Cosmos DB incidents container."""
    container = MagicMock()
    container.query_items.return_value = iter([
        {
            "id": "inc-test-001",
            "incident_id": "inc-test-001",
            "severity": "Sev1",
            "domain": "compute",
            "status": "new",
            "created_at": "2026-03-27T14:00:00Z",
            "resource_id": "/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01",
        }
    ])
    return container


@pytest.fixture()
def mock_teams_notifier():
    """Mocked Teams outbound card poster."""
    notifier = AsyncMock()
    notifier.post_card.return_value = {"message_id": "teams-msg-001"}
    notifier.update_card.return_value = {"message_id": "teams-msg-001"}
    return notifier


@pytest.fixture()
def mock_arm_client():
    """Mocked ARM resource client for Resource Identity Certainty."""
    client = MagicMock()
    client.resources.get_by_id.return_value = MagicMock(
        id="/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01",
        properties={"provisioningState": "Succeeded"},
        tags={"environment": "production"},
    )
    return client


@pytest.fixture()
def sample_approval_record():
    """Pre-built approval record dict matching D-12 schema."""
    return {
        "id": "appr_test-001",
        "action_id": "act_test-001",
        "thread_id": "thread-test-001",
        "incident_id": "inc-test-001",
        "agent_name": "compute",
        "status": "pending",
        "risk_level": "high",
        "proposed_at": "2026-03-27T14:30:00Z",
        "expires_at": "2026-03-27T15:00:00Z",
        "decided_at": None,
        "decided_by": None,
        "executed_at": None,
        "abort_reason": None,
        "resource_snapshot": {
            "resource_id": "/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01",
            "provisioning_state": "Succeeded",
            "tags": {"environment": "production"},
            "resource_health": "Available",
            "snapshot_hash": "a" * 64,
        },
        "proposal": {
            "description": "Restart VM vm-prod-01",
            "target_resources": ["/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01"],
            "estimated_impact": "~2 min downtime",
            "risk_level": "high",
            "reversibility": "reversible",
            "action_type": "restart",
        },
    }


@pytest.fixture()
def sample_remediation_proposal():
    """Pre-built RemediationProposal for testing."""
    from agents.shared.triage import RemediationProposal
    return RemediationProposal(
        description="Restart VM vm-prod-01",
        target_resources=["/subscriptions/sub-1/resourceGroups/rg-1/providers/Microsoft.Compute/virtualMachines/vm-prod-01"],
        estimated_impact="~2 min downtime",
        risk_level="high",
        reversibility="reversible",
        action_type="restart",
    )


@pytest.fixture()
def pre_seeded_embeddings():
    """3 pre-computed 1536-dim float vectors for runbook RAG testing.

    These are deterministic vectors — no Azure OpenAI call needed.
    Vector 0: "VM high CPU" topic (mostly 0.1 with peaks at positions 0-50)
    Vector 1: "NSG misconfiguration" topic (mostly 0.1 with peaks at 100-150)
    Vector 2: "Storage throttling" topic (mostly 0.1 with peaks at 200-250)
    """
    import random
    random.seed(42)
    vectors = []
    for offset in [0, 100, 200]:
        v = [0.1] * 1536
        for i in range(offset, offset + 50):
            v[i] = random.uniform(0.7, 1.0)
        # Normalize to unit vector
        magnitude = sum(x**2 for x in v) ** 0.5
        v = [x / magnitude for x in v]
        vectors.append(v)
    return vectors
```

Update `pyproject.toml` to add Phase 5 markers:
Add to the `markers` list:
```
    "e2e: End-to-end tests with Playwright",
    "sc1: Success Criterion 1 — FMP and first token latency",
    "sc2: Success Criterion 2 — SSE reconnect continuity",
    "sc3: Success Criterion 3 — Runbook RAG similarity and latency",
    "sc4: Success Criterion 4 — HITL gate lifecycle",
    "sc5: Success Criterion 5 — Resource Identity Certainty",
    "sc6: Success Criterion 6 — GitOps vs direct-apply path",
```
</action>

<acceptance_criteria>
- `services/api-gateway/tests/conftest.py` contains `def client()`
- `services/api-gateway/tests/conftest.py` contains `def mock_foundry_client()`
- `services/api-gateway/tests/conftest.py` contains `def mock_cosmos_approvals()`
- `services/api-gateway/tests/conftest.py` contains `def mock_teams_notifier()`
- `services/api-gateway/tests/conftest.py` contains `def mock_arm_client()`
- `services/api-gateway/tests/conftest.py` contains `def pre_seeded_embeddings()`
- `services/api-gateway/tests/conftest.py` contains `def sample_approval_record()`
- `pyproject.toml` contains `"sc1:` marker
- `pyproject.toml` contains `"sc5:` marker
</acceptance_criteria>

---

### Task 5-00-04: Create All Python Test Stub Files

<read_first>
- .planning/phases/05-triage-remediation-web-ui/05-VALIDATION.md §"Per-Task Verification Map" (full list)
- .planning/phases/05-triage-remediation-web-ui/05-RESEARCH.md §"Validation Architecture" (SC-1 through SC-6 acceptance conditions)
- services/api-gateway/tests/test_incidents.py (existing test pattern)
</read_first>

<action>
Create the following stub test files. Every test method is `@pytest.mark.skip(reason="Wave 0 stub — implemented in Plan 05-XX")`:

**`services/api-gateway/tests/test_chat_endpoint.py`:**
- `TestChatEndpoint.test_valid_chat_creates_thread` — POST /api/v1/chat returns 202 with thread_id
- `TestChatEndpoint.test_chat_requires_message` — POST /api/v1/chat without message returns 422
- `TestChatEndpoint.test_chat_attaches_to_existing_incident` — POST with incident_id reuses thread

**`services/api-gateway/tests/test_sse_stream.py`:**
- `TestSSEStream.test_monotonic_sequence_numbers` — seq values are strictly increasing
- `TestSSEStream.test_heartbeat_interval_20_seconds` — heartbeat comment sent within 25s
- `TestSSEStream.test_last_event_id_reconnect_replays_missed` — events since Last-Event-ID replayed
- `TestSSEStream.test_ring_buffer_evicts_oldest` — buffer of 1000, insert 1100, verify oldest evicted
- `TestSSEStream.test_two_event_types_token_and_trace` — both event:token and event:trace in stream

**`services/api-gateway/tests/test_runbook_rag.py`:**
- `TestRunbookRAG.test_search_returns_top_3_results` — GET /api/v1/runbooks/search returns max 3
- `TestRunbookRAG.test_similarity_above_075_threshold` — all results have similarity >= 0.75
- `TestRunbookRAG.test_search_latency_under_500ms` — end-to-end < 500ms
- `TestRunbookRAG.test_domain_filter_applied` — domain=compute only returns compute runbooks
- `TestRunbookRAG.test_citation_includes_title_and_version` — result has title and version fields

**`services/api-gateway/tests/test_approval_lifecycle.py`:**
- `TestApprovalLifecycle.test_create_pending_approval` — creates record with status pending
- `TestApprovalLifecycle.test_approve_pending_sets_approved` — pending -> approved transition
- `TestApprovalLifecycle.test_reject_pending_sets_rejected` — pending -> rejected transition
- `TestApprovalLifecycle.test_expired_approval_returns_410` — expired proposal returns 410 Gone
- `TestApprovalLifecycle.test_expired_never_executed` — approved record past expires_at never runs
- `TestApprovalLifecycle.test_thread_not_polled_after_park` — create_run not called after proposal
- `TestApprovalLifecycle.test_thread_resume_on_webhook` — approval webhook resumes Foundry thread
- `TestApprovalLifecycle.test_etag_concurrency_on_write` — replace_item called with match_condition

**`services/api-gateway/tests/test_resource_identity.py`:**
- `TestResourceIdentity.test_snapshot_hash_is_sha256_64_chars` — hash format check
- `TestResourceIdentity.test_identity_match_returns_true` — identical state -> True
- `TestResourceIdentity.test_diverged_state_returns_false` — changed tags -> False
- `TestResourceIdentity.test_stale_approval_aborts_execution` — Cosmos record abort_reason = "stale_approval"
- `TestResourceIdentity.test_snapshot_has_minimum_2_signals` — resource_id + snapshot_hash present

**`services/api-gateway/tests/test_rate_limiting.py`:**
- `TestRateLimiting.test_within_limit_allowed` — 5 actions in 1 min pass
- `TestRateLimiting.test_exceeds_limit_rejected` — 6th action in 1 min rejected with 429
- `TestRateLimiting.test_protected_tag_blocks_action` — resource with protected:true returns 403
- `TestRateLimiting.test_prod_requires_scope_confirmation` — prod sub without scope_confirmed returns 403

**`services/api-gateway/tests/test_gitops_path.py`:**
- `TestGitOpsPath.test_flux_detected_returns_gitops_managed_true` — non-empty flux config -> True
- `TestGitOpsPath.test_no_flux_returns_gitops_managed_false` — empty flux config -> False
- `TestGitOpsPath.test_gitops_path_creates_pr` — GitHub API POST /repos/.../pulls called
- `TestGitOpsPath.test_direct_apply_no_pr` — kubectl_apply called, github_create_pr NOT called
- `TestGitOpsPath.test_pr_branch_name_format` — branch matches `aiops/fix-{incident_id}-*`

**`services/api-gateway/tests/test_audit_trail.py`:**
- `TestAuditTrail.test_approval_written_to_cosmos` — Cosmos create_item called for approval
- `TestAuditTrail.test_approval_written_to_onelake` — OneLake write fires after Cosmos write
- `TestAuditTrail.test_onelake_failure_non_blocking` — OneLake error logged but no exception
- `TestAuditTrail.test_audit_query_filters_by_agent` — query with agent=compute returns only compute actions
- `TestAuditTrail.test_audit_query_filters_by_time_range` — from/to time range applied
</action>

<acceptance_criteria>
- `services/api-gateway/tests/test_chat_endpoint.py` contains `class TestChatEndpoint`
- `services/api-gateway/tests/test_sse_stream.py` contains `test_monotonic_sequence_numbers`
- `services/api-gateway/tests/test_sse_stream.py` contains `test_heartbeat_interval_20_seconds`
- `services/api-gateway/tests/test_sse_stream.py` contains `test_last_event_id_reconnect_replays_missed`
- `services/api-gateway/tests/test_runbook_rag.py` contains `test_similarity_above_075_threshold`
- `services/api-gateway/tests/test_runbook_rag.py` contains `test_search_latency_under_500ms`
- `services/api-gateway/tests/test_approval_lifecycle.py` contains `test_expired_approval_returns_410`
- `services/api-gateway/tests/test_approval_lifecycle.py` contains `test_thread_not_polled_after_park`
- `services/api-gateway/tests/test_resource_identity.py` contains `test_stale_approval_aborts_execution`
- `services/api-gateway/tests/test_resource_identity.py` contains `test_snapshot_has_minimum_2_signals`
- `services/api-gateway/tests/test_rate_limiting.py` contains `test_protected_tag_blocks_action`
- `services/api-gateway/tests/test_gitops_path.py` contains `test_pr_branch_name_format`
- `services/api-gateway/tests/test_audit_trail.py` contains `test_onelake_failure_non_blocking`
- Every test method in all stub files contains `@pytest.mark.skip`
- Running `pytest services/api-gateway/tests/ -x -q` collects all stubs with 0 failures (all skipped)
</acceptance_criteria>

---

### Task 5-00-05: Create Web UI Test Stub Files

<read_first>
- .planning/phases/05-triage-remediation-web-ui/05-VALIDATION.md §"Per-Task Verification Map" (UI test stubs)
- .planning/phases/05-triage-remediation-web-ui/05-UI-SPEC.md §"Component Inventory" (component names)
- services/web-ui/package.json (dependencies from Task 5-00-01)
</read_first>

<action>
Create `services/web-ui/__tests__/layout.test.tsx`:
```tsx
import { describe, it, expect } from '@jest/globals';

describe('AppLayout', () => {
  it.skip('renders split-pane with chat and dashboard panels', () => {
    // TODO: Plan 05-01
  });

  it.skip('chat panel has minimum 25% width', () => {
    // TODO: Plan 05-01
  });

  it.skip('dashboard panel has minimum 40% width', () => {
    // TODO: Plan 05-01
  });

  it.skip('default split is 35% chat / 65% dashboard', () => {
    // TODO: Plan 05-01
  });

  it.skip('shows desktop-only message below 1200px', () => {
    // TODO: Plan 05-01
  });

  it.skip('right pane has Alerts, Topology, Resources, Audit Log tabs', () => {
    // TODO: Plan 05-01
  });

  it.skip('Alerts tab is selected by default', () => {
    // TODO: Plan 05-01
  });
});
```

Create `services/web-ui/__tests__/auth.test.tsx`:
```tsx
import { describe, it, expect } from '@jest/globals';

describe('MSAL PKCE Auth', () => {
  it.skip('MsalProvider wraps application root', () => {
    // TODO: Plan 05-01
  });

  it.skip('UnauthenticatedTemplate shows login button', () => {
    // TODO: Plan 05-01
  });

  it.skip('AuthenticatedTemplate shows main layout', () => {
    // TODO: Plan 05-01
  });
});
```
</action>

<acceptance_criteria>
- `services/web-ui/__tests__/layout.test.tsx` contains `renders split-pane with chat and dashboard panels`
- `services/web-ui/__tests__/layout.test.tsx` contains `it.skip`
- `services/web-ui/__tests__/layout.test.tsx` contains `Alerts tab is selected by default`
- `services/web-ui/__tests__/auth.test.tsx` contains `MsalProvider wraps application root`
</acceptance_criteria>

---

### Task 5-00-06: Create SSE Heartbeat Test Stub File

<read_first>
- .planning/phases/05-triage-remediation-web-ui/05-VALIDATION.md §"Wave 0 Requirements" (test_sse_heartbeat.py)
- .planning/phases/05-triage-remediation-web-ui/05-CONTEXT.md D-08 (20-second heartbeat, Container Apps 240s timeout)
</read_first>

<action>
Create `services/api-gateway/tests/test_sse_heartbeat.py`:

```python
"""Stub: SSE heartbeat interval test (UI-008)."""
import pytest


class TestSSEHeartbeat:
    """Tests for SSE heartbeat comment emission (UI-008).

    The SSE route handler must emit a `: heartbeat` comment every 20 seconds
    to prevent Azure Container Apps 240-second idle timeout from terminating
    the connection.
    """

    @pytest.mark.skip(reason="stub - implement in Plan 05-02")
    def test_heartbeat_sent_every_20_seconds(self):
        """Assert that the SSE route emits a :heartbeat comment every 20s."""
        pass

    @pytest.mark.skip(reason="stub - implement in Plan 05-02")
    def test_heartbeat_prevents_container_app_timeout(self):
        """Assert heartbeat is sent before 240s Container Apps timeout."""
        pass
```
</action>

<acceptance_criteria>
- `services/api-gateway/tests/test_sse_heartbeat.py` contains `class TestSSEHeartbeat`
- `services/api-gateway/tests/test_sse_heartbeat.py` contains `test_heartbeat_sent_every_20_seconds`
- `services/api-gateway/tests/test_sse_heartbeat.py` contains `test_heartbeat_prevents_container_app_timeout`
- `services/api-gateway/tests/test_sse_heartbeat.py` contains `@pytest.mark.skip`
- `services/api-gateway/tests/test_sse_heartbeat.py` contains `UI-008`
</acceptance_criteria>

---

## Verification

After all tasks complete:

1. `pytest services/api-gateway/tests/ --co -q` collects all stub tests (should report 40+ tests collected, all skipped)
2. `services/web-ui/package.json` is valid JSON parseable by `node -e "require('./services/web-ui/package.json')"`
3. `services/web-ui/playwright.config.ts` exists
4. All 4 E2E stub files exist under `e2e/`
5. `pyproject.toml` contains the 6 new SC markers
6. `services/api-gateway/tests/test_sse_heartbeat.py` exists with 2 skipped tests

---

## must_haves

- [ ] All VALIDATION.md Wave 0 test stubs are created
- [ ] Shared conftest.py has mock fixtures for Foundry, Cosmos (approvals + incidents), Teams, ARM, pgvector embeddings
- [ ] Playwright config targeting localhost:3000 with tagged test suites
- [ ] Web UI package.json with exact Fluent UI v9 9.73.4, MSAL v3, Next.js 15, Playwright 1.58.2
- [ ] pyproject.toml updated with SC markers
- [ ] test_sse_heartbeat.py stub created for UI-008 heartbeat validation
