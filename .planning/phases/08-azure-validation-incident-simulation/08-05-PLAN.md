---
plan: 08-05
title: Validation Closeout
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md
  - .planning/STATE.md
autonomous: false
---

# Plan 08-05: Validation Closeout

> Wave 4 — depends on Plans 03 and 04 (all simulation and deferred work complete).
> Finalizes the VALIDATION-REPORT.md, logs backlog items, and verifies phase completion.

## must_haves

- [ ] VALIDATION-REPORT.md has all findings with final FIXED/OPEN status
- [ ] Zero BLOCKING findings remain OPEN
- [ ] All DEGRADED/COSMETIC findings have been logged as backlog todos
- [ ] OTel spans verified in Application Insights (manual check documented)
- [ ] STATE.md updated with Phase 8 status

---

## Tasks

<task id="08-05-01">
<title>Finalize VALIDATION-REPORT.md with all findings and OTel verification</title>
<wave>4</wave>
<read_first>
- .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md
- .planning/phases/08-azure-validation-incident-simulation/08-CONTEXT.md (D-08, D-09, D-10)
</read_first>
<action>
Review and finalize `.planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md`:

1. **Update all finding statuses** — go through every row in the Findings table:
   - Findings fixed during Phase 8: change Status from `OPEN` to `FIXED`
   - Findings not fixed: keep as `OPEN` with clear description of what's needed

2. **Add OTel Verification section** after Simulation Results:
   ```markdown
   ## OTel Manual Span Verification

   | Span Type | Expected | Verified? | Notes |
   |-----------|----------|-----------|-------|
   | `foundry.create_thread` | App Insights Transaction Search | Y/N | |
   | `foundry.post_message` | App Insights Transaction Search | Y/N | |
   | `foundry.create_run` | App Insights Transaction Search | Y/N | |
   | `foundry.list_messages` | App Insights Transaction Search | Y/N | |
   | `mcp.tool_approval` | App Insights Transaction Search | Y/N | |
   | `agent.orchestrator` | App Insights Transaction Search | Y/N | |
   ```

   To verify: Azure Portal > Application Insights > Transaction Search > filter by
   operation name starting with `foundry.` / `mcp.` / `agent.orchestrator`.

   Note: The span name pattern is `agent.{agent_name}` (e.g. `agent.orchestrator`,
   `agent.compute`), NOT a fixed `agent.invoke`. Query for `agent.orchestrator`
   explicitly to verify the orchestrator dispatch path.

3. **Update Summary section** with final counts:
   ```markdown
   ## Summary

   - **BLOCKING:** X findings (all FIXED / Y remain OPEN)
   - **DEGRADED:** X findings (logged as backlog)
   - **COSMETIC:** X findings (logged as backlog)
   - **E2E Tests:** X/5 passed
   - **Smoke Tests:** X/7 passed
   - **Simulations:** X/8 passed (7 scenarios, cross-domain counts as 2)
   - **OTel Spans:** X/6 verified in App Insights
   - **Overall:** PASS/FAIL
   ```

4. **Add Conclusion section**:
   ```markdown
   ## Conclusion

   Phase 8 validation [PASSED/FAILED]. [Summary of what was proven and what gaps remain.]

   ### Backlog Items Created
   - [ ] [Description of DEGRADED finding 1]
   - [ ] [Description of DEGRADED finding 2]
   - [ ] [Description of COSMETIC finding 1]
   ```

NOTE: If Azure Portal access is not available to verify OTel spans, mark as CANNOT_VERIFY and note it in the report.
</action>
<acceptance_criteria>
- `08-VALIDATION-REPORT.md` contains `## OTel Manual Span Verification` section with 6 rows
- `08-VALIDATION-REPORT.md` OTel section queries for `agent.orchestrator` (not `agent.invoke`)
- `08-VALIDATION-REPORT.md` contains `## Summary` section with final counts for BLOCKING, DEGRADED, COSMETIC
- `08-VALIDATION-REPORT.md` contains `## Conclusion` section
- Every row in the `## Findings` table has a Status of either `FIXED`, `OPEN`, or `CANNOT_VERIFY` (no blank statuses)
- `08-VALIDATION-REPORT.md` contains `### Backlog Items Created` with at least a placeholder list
</acceptance_criteria>
</task>

<task id="08-05-02">
<title>Log DEGRADED/COSMETIC findings as backlog items</title>
<wave>4</wave>
<depends_on>08-05-01</depends_on>
<read_first>
- .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md (after task 08-05-01)
</read_first>
<action>
For each finding in the VALIDATION-REPORT.md with severity DEGRADED or COSMETIC and status OPEN:

1. Extract the finding description, service, and any fix notes
2. Log as a backlog todo using the GSD system

Format for each backlog item:
```
Title: [Phase 8 Finding F-XX] <Service>: <Description>
Severity: DEGRADED | COSMETIC
Source: 08-VALIDATION-REPORT.md finding F-XX
```

If no DEGRADED or COSMETIC findings exist (all passed or all fixed), document this as:
```
No DEGRADED or COSMETIC findings to log — all validation checks passed or were fixed during Phase 8.
```

Update the `### Backlog Items Created` section in the VALIDATION-REPORT.md with the actual items created.
</action>
<acceptance_criteria>
- Each DEGRADED/COSMETIC finding has a corresponding backlog item created (or documented as "none needed")
- `08-VALIDATION-REPORT.md` `### Backlog Items Created` section lists the actual items (not placeholders)
</acceptance_criteria>
</task>

<task id="08-05-03">
<title>Verify no BLOCKING findings remain open and update STATE.md</title>
<wave>4</wave>
<depends_on>08-05-01,08-05-02</depends_on>
<read_first>
- .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md
- .planning/STATE.md
</read_first>
<action>
1. **Verify BLOCKING check**: Run this assertion against the report:
   ```bash
   grep -c "BLOCKING.*OPEN" .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md
   ```
   Expected: 0 (no BLOCKING findings remain OPEN)

   If any BLOCKING findings remain OPEN, Phase 8 CANNOT be marked complete. Document which BLOCKING items need resolution and stop here.

2. **Update STATE.md**: Add Phase 8 to the project state:

   Update the frontmatter:
   ```yaml
   progress:
     total_phases: 8
     completed_phases: 8  # (or 7 if BLOCKING issues remain)
     total_plans: 36  # (31 + 5 new)
     completed_plans: 36  # (or actual count)
   ```

   Update the current phase section:
   ```markdown
   ## Current Phase

   **Phase 8: Azure Validation & Incident Simulation — ✅ COMPLETE (5/5 plans)**

   Plan 08-01 complete: Fix Provisioning Gaps — Foundry Orchestrator Agent created, ORCHESTRATOR_AGENT_ID set on ca-api-gateway-prod, Azure AI Developer RBAC granted, CORS locked to prod URL, Azure Bot Service registered, 3 GitHub secrets added.

   Plan 08-02 complete: Critical-Path Validation — test.skip() removed from E2E tests, E2E suite run against prod, 7 smoke tests executed, VALIDATION-REPORT.md initialized.

   Plan 08-03 complete: Incident Simulation — 7 scenario scripts + common utilities + run-all.sh orchestrator, simulation CI gate wired into staging-e2e-simulation.yml, all scenarios executed against prod.

   Plan 08-04 complete: Deferred Phase 7 Work — instrumentation.py with foundry_span/mcp_span/agent_span (span pattern: agent.{agent_name}), manual OTel spans added to foundry.py/chat.py/approvals.py, api-gateway redeployed, e2e-teams-roundtrip.spec.ts created.

   Plan 08-05 complete: Validation Closeout — VALIDATION-REPORT.md finalized, backlog items logged, no BLOCKING findings.
   ```

   Add to Phase Summary table:
   ```markdown
   | 8 | Azure Validation & Incident Simulation | ✅ Complete (2026-03-29) — all 5 plans, 7 simulations, manual OTel spans |
   ```

3. **Commit phase artifacts**: Stage and commit all Phase 8 files:
   - `.planning/phases/08-azure-validation-incident-simulation/08-*.md` (all plan files + report)
   - `scripts/simulate-incidents/` (all simulation files)
   - `services/api-gateway/instrumentation.py`
   - Modified: `services/api-gateway/foundry.py`, `chat.py`, `approvals.py`
   - Modified: `e2e/e2e-incident-flow.spec.ts`, `e2e-hitl-approval.spec.ts`, `e2e-sse-reconnect.spec.ts`
   - New: `e2e/e2e-teams-roundtrip.spec.ts`
   - Modified: `scripts/configure-orchestrator.py`
   - Modified: `.github/workflows/staging-e2e-simulation.yml`
</action>
<acceptance_criteria>
- `grep -c "BLOCKING.*OPEN" .planning/phases/08-azure-validation-incident-simulation/08-VALIDATION-REPORT.md` returns `0`
- `.planning/STATE.md` contains `Phase 8: Azure Validation & Incident Simulation`
- `.planning/STATE.md` Phase Summary table has 8 rows
- `.planning/STATE.md` frontmatter `total_phases` is `8`
- `.planning/STATE.md` contains `completed_phases: 8` (or 7 if BLOCKING issues remain, in which case acceptance fails)
</acceptance_criteria>
</task>
