# Pinned base requirements for all AAP agent containers
# Update versions here to propagate to all agents

# Microsoft Agent Framework — pinned to beta that has ChatAgent/ai_function API.
# rc5 introduced a breaking API overhaul (Agent/tool/WorkflowBuilder) that is
# incompatible with agentserver-agentframework b10-b15. agentserver b16+ requires
# agent-framework-core>=rc2 (rc5 API). Use b15 stack: last version compatible with
# the b260107 beta API AND the lifespan (not on_event) starlette integration.
agent-framework==1.0.0b260107

# Foundry Hosted Agent adapters
# Compatibility: b15 is the pivot version:
#   - core b9-b14: uses @app.on_event("startup") removed in starlette 0.21; ships
#     starlette>=0.45 requirement but code fails at runtime → never use b14 or earlier
#   - core b15+: uses starlette lifespan (correct); requires azure-ai-projects>=2.0.0b1
#   - agentframework b15: last version requiring agent-framework-core<=b260107
#   - agentframework b16+: requires agent-framework-core>=rc2 (rc5 API)
azure-ai-agentserver-core==1.0.0b15
azure-ai-agentserver-agentframework==1.0.0b15

# Azure AI Foundry SDK
# agentserver-core b15 requires azure-ai-projects>=2.0.0b1 and azure-ai-agents==1.2.0b5.
# azure-ai-projects 2.0.0b3 has MCPTool (needed by arc/patch) and PromptAgentDefinitionText
# (needed by agent_framework_azure_ai companion to agent-framework b260107).
azure-ai-projects==2.0.0b3
azure-ai-agents==1.2.0b5

# Azure Identity (managed identity via IMDS)
azure-identity>=1.17.0

# Cosmos DB SDK (session budget tracking)
azure-cosmos>=4.7.0

# OpenTelemetry + Azure Monitor
azure-monitor-opentelemetry>=1.6.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0

# Data validation
pydantic>=2.8.0
