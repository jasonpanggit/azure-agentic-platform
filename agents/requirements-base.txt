# Pinned base requirements for all AAP agent containers
# Update versions here to propagate to all agents

# Microsoft Agent Framework — pinned to beta that has ChatAgent/ai_function/HandoffBuilder API.
# rc5 introduced a breaking API overhaul (Agent/tool/WorkflowBuilder) that is incompatible
# with the current azure-ai-agentserver-agentframework release. Pin to the last beta that
# has the original API AND is compatible with azure-ai-agentserver-agentframework b9.
# Upgrade path: wait for agentserver b10+ that supports agent-framework rc5+.
agent-framework==1.0.0b260107

# Azure AI Foundry SDK (GA)
# azure-ai-projects 2.x removed thread/run/message operations — use azure-ai-agents
azure-ai-projects>=2.0.1
azure-ai-agents>=1.0.0

# Foundry Hosted Agent adapters — b9 is compatible with agent-framework b260107
azure-ai-agentserver-core>=1.0.0b9
azure-ai-agentserver-agentframework==1.0.0b9

# Azure Identity (managed identity via IMDS)
azure-identity>=1.17.0

# Cosmos DB SDK (session budget tracking)
azure-cosmos>=4.7.0

# OpenTelemetry + Azure Monitor
azure-monitor-opentelemetry>=1.6.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0

# MCP SDK (tool surface)
mcp[cli]>=1.26.0

# Data validation
pydantic>=2.8.0
