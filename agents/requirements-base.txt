# Pinned base requirements for all AAP agent containers
# Update versions here to propagate to all agents

# Microsoft Agent Framework (RC — pin exactly)
agent-framework==1.0.0rc5

# Azure AI Foundry SDK (GA)
# azure-ai-projects 2.x removed thread/run/message operations — use azure-ai-agents
azure-ai-projects>=2.0.1
azure-ai-agents>=1.0.0

# Foundry Hosted Agent adapters
# PINNED: 1.0.0b17 is the latest available (released 2026-03-16).
# WARNING: 1.0.0b17 declares agent-framework-core>=1.0.0rc2,<=1.0.0rc3 but this repo
# pins agent-framework==1.0.0rc5. These packages need to release a b18+ that supports rc4/rc5.
# Monitor https://pypi.org/project/azure-ai-agentserver-agentframework/ and upgrade together.
# Do NOT change either pin in isolation — upgrade agent-framework + agentserver as a matched pair.
azure-ai-agentserver-core==1.0.0b17
azure-ai-agentserver-agentframework==1.0.0b17

# Azure Identity (managed identity via IMDS)
azure-identity>=1.17.0

# Cosmos DB SDK (session budget tracking)
azure-cosmos>=4.7.0

# OpenTelemetry + Azure Monitor
azure-monitor-opentelemetry>=1.6.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0

# MCP SDK (tool surface)
mcp[cli]>=1.26.0

# Data validation
pydantic>=2.8.0
