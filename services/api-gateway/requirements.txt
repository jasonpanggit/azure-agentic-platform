# API Gateway dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Azure authentication
azure-identity>=1.17.0
fastapi-azure-auth>=5.0.0

# Azure AI Foundry SDK
# azure-ai-projects 2.x removed thread/run/message operations — use azure-ai-agents instead
# 1.2.0b5 required for SubmitToolApprovalAction (MCP tool approval gate)
azure-ai-agents==1.2.0b5

# MCP client — stdio transport to @azure/mcp subprocess (gcc-demo pattern)
mcp[cli]>=1.26.0

# Azure Cosmos DB SDK
azure-cosmos>=4.0.0

# OpenTelemetry auto-instrumentation (D-05)
azure-monitor-opentelemetry>=1.0.0

# OneLake / Azure Data Lake Storage Gen2
azure-storage-file-datalake>=12.0.0

# Runbook RAG — pgvector semantic search
openai>=1.0.0
asyncpg>=0.29.0
pgvector>=0.3.0

# HTTP client for endoflife.date API fallback (EOL endpoints)
httpx>=0.27.0

# Azure Resource Graph — patch assessment + installation queries (Phase 13)
azure-mgmt-resourcegraph>=8.0.0

# Azure Monitor Query — Log Analytics workspace queries (ConfigurationData enrichment)
azure-monitor-query>=1.4.0

# Azure Log Analytics management — workspace customerId (GUID) lookup for Change Tracking discovery
azure-mgmt-loganalytics>=13.0.0

# Diagnostic Pipeline — Azure Monitor + Resource Health (Phase 15)
azure-mgmt-monitor>=6.0.0
azure-mgmt-resourcehealth==1.0.0b6

# VM Cost Summary — Azure Advisor rightsizing recommendations (Phase 39)
azure-mgmt-advisor>=9.0.0

# Test dependencies
pytest-asyncio>=0.23.0
